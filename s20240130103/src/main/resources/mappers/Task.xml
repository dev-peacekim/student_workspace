<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blackberry.s20240130103.TaskMapper">
					
	<select id="kphUnCompTaskListByProjectNo" parameterType="Long" resultType="KphTask">
		SELECT *
		FROM TASK
		WHERE PROJECT_NO = #{project_no}
		AND TASK_COMP_CHK = 0
		ORDER BY TASK_END
	</select>
	
	<select id="kphCompTaskListByProjectNo" parameterType="Long" resultType="KphTask">
		SELECT *
		FROM TASK
		WHERE PROJECT_NO = #{project_no}
		AND TASK_COMP_CHK = 1
		ORDER BY TASK_END
	</select>
	
	<select id="kphTaskListByProjectNo" parameterType="KphTask" resultType="KphTask">
		SELECT * 
		FROM TASK
		WHERE PROJECT_NO = #{project_no}
		ORDER BY TASK_END
	</select>
	
	<insert id="kphTaskInsert" parameterType="KphTask" useGeneratedKeys="true" keyColumn="task_no" keyProperty="task_no">
		INSERT INTO TASK t
		VALUES (#{project_no}, (SELECT NVL(MAX(TASK_NO), 0)+1 FROM TASK t2 WHERE PROJECT_NO = #{project_no}), '#{task_title}', #{task_start}, #{task_end}, 0, NULL)
	</insert>
	
</mapper>